# Domain

Pure TypeScript business rules belong here: counter bounds, section-tree construction and completion, lifecycle transformations, quantity validation, and URL validation. Domain modules must not import React or Firebase.
