# Firebase adapters

Firebase initialization, Authentication, Firestore converters and repositories, emulator connections, timestamp handling, and Firebase error translation belong here. Cloudinary photo operations live in `lib/cloudinary` so the two services remain distinct.
